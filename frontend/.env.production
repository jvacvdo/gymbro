# Vacio a proposito: frontend y backend se despliegan juntos en Emergent,
# y su ingress enruta /api al backend. Dejarlo vacio hace que las llamadas
# salgan al mismo dominio, asi que la URL de produccion no hay que tocarla
# despues de cada despliegue.
VITE_API_URL=
